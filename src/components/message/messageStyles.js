import {StyleSheet} from 'react-native'

const Styles = StyleSheet.create({
    messageContent:{
        fontSize: 15,
        color: '#fff',
        margin: 10,
        borderColor: '#fff',
        borderWidth: 1,
        padding: 10,
        paddingLeft: 30,
        borderRadius: 50,
    },
    messageView:{

    }
})

export default Styles
